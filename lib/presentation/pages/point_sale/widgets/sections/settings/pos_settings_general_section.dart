import 'package:flutter/material.dart';

class PosSettingsGeneralSection extends StatelessWidget {
  const PosSettingsGeneralSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Configuración general (pendiente)'));
  }
}